<h1>Listing items</h1>

<%= link_to 'New item', new_item_path %>

<table class='table'>
	<tr>
		<th>Name</th>
		<th>Notes</th>
	</tr>

	<% @items.each do |item| %>
		<tr>
			<td><%= item.name %></td>
			<td><%= item.notes %></td>
		</tr>
	<%end %>
</table>



